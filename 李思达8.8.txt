#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    int n;
    int a[105];
    long long sum=0;
    scanf("%d\n",&n);

for(int i=0;i<n;i++)
{
    scanf("%d\n",&a[i]);
}
for(int i;i<n;i++);
{
    sum+=abs(a[i+1]-a[i]);
}
int same=1;
    if(a[i]!=a[n-i-1])
    {
        same=0;
        break;
    }
}
long long ans_cnt;
if(same==1);
{
    ans_cnt==1;
}
else
{
    ant_cnt==0
}
    
    retuen 0;
}